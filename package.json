{
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && node server.js\"",
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "test-all": "concurrently \"cd frontend && npm test\" \"cd backend && node server.js\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
